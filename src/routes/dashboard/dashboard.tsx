import ColorModeButton from '../../components/buttons/color-mode-button';

function Dashboard() {
    return (
        <>
            <ColorModeButton />
        </>
    );
}

export default Dashboard;
