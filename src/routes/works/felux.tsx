import WorkPage from '../../components/work-page';

export default function Felux() {
    const points: string[] = [
        // TODO: Add key achievements
    ];

    return (
        <WorkPage
            company={'Felux'}
            title={''}
            dates={'Aug 2025 - Present'}
            location={''}
            imageUrl={''}
            points={points}
        >
            {/* TODO: Add experience content */}
        </WorkPage>
    );
}
